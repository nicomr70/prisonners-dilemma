spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dilemme
    username: postgres
    password: 292424
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
